# Active Directory Lab Generator

This project automates the creation of a randomized Active Directory environment using PowerShell.

## Features
- Random user and group generation
- JSON-based configuration
- Automated AD deployment
- Undo/reset functionality

## Workflow
data → random_domaine.ps1 → JSON → gen_ad.ps1 → Active Directory
